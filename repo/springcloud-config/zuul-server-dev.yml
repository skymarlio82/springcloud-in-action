zuul:
  routes:
    config-client:
      path: /api_cnf_cli/**
      service-id: config-client
    user-service:
      path: /api_user/**
      service-id: user-service
url-whitelist:
  - /user/auth/login
  - /user/token/check