server:
  port: 8764
spring:
  application:
    name: hystrix-dashboard-server
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
feign:
  hystrix:
    enabled: true